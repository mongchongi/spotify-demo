import { Typography } from '@mui/material';

const Albums = () => {
  return (
    <div>
      <Typography variant='h1' fontWeight={700}>
        Albums
      </Typography>
    </div>
  );
};

export default Albums;
